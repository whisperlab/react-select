import { Component } from 'react';

import { type AsyncProps, defaultProps } from '@whisperlab/react-select/src/Async';

export default class Select extends Component<AsyncProps> {
  defaultProps = defaultProps;
}
